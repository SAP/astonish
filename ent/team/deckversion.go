// Code generated by ent, DO NOT EDIT.

package team

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SAP/astonish/ent/team/deckversion"
	"github.com/google/uuid"
)

// DeckVersion is the model entity for the DeckVersion schema.
type DeckVersion struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// DeckSlug holds the value of the "deck_slug" field.
	DeckSlug string `json:"deck_slug,omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Snapshot holds the value of the "snapshot" field.
	Snapshot string `json:"snapshot,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeckVersion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deckversion.FieldVersion:
			values[i] = new(sql.NullInt64)
		case deckversion.FieldDeckSlug, deckversion.FieldTitle, deckversion.FieldSnapshot:
			values[i] = new(sql.NullString)
		case deckversion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case deckversion.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeckVersion fields.
func (_m *DeckVersion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deckversion.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case deckversion.FieldDeckSlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field deck_slug", values[i])
			} else if value.Valid {
				_m.DeckSlug = value.String
			}
		case deckversion.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = int(value.Int64)
			}
		case deckversion.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case deckversion.FieldSnapshot:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot", values[i])
			} else if value.Valid {
				_m.Snapshot = value.String
			}
		case deckversion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeckVersion.
// This includes values selected through modifiers, order, etc.
func (_m *DeckVersion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this DeckVersion.
// Note that you need to call DeckVersion.Unwrap() before calling this method if this DeckVersion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DeckVersion) Update() *DeckVersionUpdateOne {
	return NewDeckVersionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DeckVersion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DeckVersion) Unwrap() *DeckVersion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("team: DeckVersion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DeckVersion) String() string {
	var builder strings.Builder
	builder.WriteString("DeckVersion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("deck_slug=")
	builder.WriteString(_m.DeckSlug)
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("snapshot=")
	builder.WriteString(_m.Snapshot)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DeckVersions is a parsable slice of DeckVersion.
type DeckVersions []*DeckVersion
